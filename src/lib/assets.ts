// Public assets live under the app's base URL ("/admin/"). Using BASE_URL keeps
// them resolving correctly whether the app is served at / (dev) or /admin (prod).
export const LOGO_URL = `${import.meta.env.BASE_URL}conquest-logo.webp`
